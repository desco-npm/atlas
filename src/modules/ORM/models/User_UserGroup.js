module.exports = async ({ DataTypes, Orm,  }) => {
  const defs = {}

  const opts = {}

  const pos = () => {}

  const mixins = []

  return Orm.addModel({ name: Orm.User_UserGroup, defs, opts, pos, mixins, })
}