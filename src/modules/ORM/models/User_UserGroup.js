module.exports = async ({ DataTypes, Orm,  }) => {
  const attrs = {}

  const opts = {}

  const pos = () => {}

  const mixins = []

  return Orm.addModel({ name: Orm.User_UserGroup, attrs, opts, pos, mixins, })
}